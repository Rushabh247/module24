// Call the function before its declaration
console.log(multiplyNumbers(5, 3)); // This will output 15

// Function expression (assigned to a variable)
var multiplyNumbers = function(a, b) {
    return a * b;
};
